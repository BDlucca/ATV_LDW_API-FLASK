[project]
name = "ATV_LDW_API-FLASK"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pydantic>=2.12.5",
]

[tool.uv.workspace]
members = [
    "apps/backend",
]
